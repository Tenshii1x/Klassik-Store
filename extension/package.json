{
  "name": "klassik-store-extension",
  "version": "1.0.0",
  "description": "Chrome extension to import Temu products into Klassik Store admin",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "icons": "node scripts/make-icons.cjs",
    "package": "npm run icons && npm run build && node scripts/package-zip.cjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
